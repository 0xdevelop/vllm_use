import React from 'react';import{createRoot}from'react-dom/client';import{QueryClient,QueryClientProvider}from'@tanstack/react-query';import{createRootRoute,createRoute,createRouter,Outlet,RouterProvider}from'@tanstack/react-router';import App from './App';import{pages}from'./pages';import'./styles.css'
const root=createRootRoute({component:()=> <App><Outlet/></App>});const routes=pages.map(p=>createRoute({getParentRoute:()=>root,path:p.path,component:p.component}));const index=createRoute({getParentRoute:()=>root,path:'/',component:pages[0]!.component});const tree=root.addChildren([index,...routes]);const router=createRouter({routeTree:tree});declare module '@tanstack/react-router'{interface Register{router:typeof router}}
createRoot(document.getElementById('root')!).render(<React.StrictMode><QueryClientProvider client={new QueryClient({defaultOptions:{queries:{retry:1,refetchInterval:10000}}})}><RouterProvider router={router}/></QueryClientProvider></React.StrictMode>)
